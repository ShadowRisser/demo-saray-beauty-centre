const reveal=()=>document.querySelectorAll('.reveal').forEach(el=>{if(el.getBoundingClientRect().top<innerHeight-70)el.classList.add('show')});addEventListener('scroll',reveal,{passive:true});addEventListener('load',reveal);document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();f.parentElement.querySelector('.success').classList.add('show');}));document.querySelectorAll('figure').forEach((f,i)=>{f.style.transitionDelay=(i*.08)+'s'});