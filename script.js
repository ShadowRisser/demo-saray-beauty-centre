const reveal=()=>{document.querySelectorAll('.reveal,.shot').forEach(el=>{const r=el.getBoundingClientRect();if(r.top<innerHeight-70)el.classList.add('show')})};addEventListener('scroll',reveal);addEventListener('load',reveal);document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();f.parentElement.querySelector('.thanks').classList.add('show')}));document.addEventListener('mousemove',e=>{const c=document.querySelector('.cursor');if(c){c.style.transform=`translate(${e.clientX}px,${e.clientY}px)`}});